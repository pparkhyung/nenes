<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/mvc
		http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- RequestMapping에 해당하지 않는 img,js,css를 서비스하기 위하여 선언을 추가해야 한다. -->
	<mvc:default-servlet-handler />

	<mvc:annotation-driven />

	<!-- bean 주입을 위한 @Autowired, @Resource 등을 사용하능하게 한다. 사용을 위하여 beans 태그에 context 
		관련 선언을 추가해야 한다. -->
	<context:annotation-config />

    <!-- Command 패키지 이하의 클래스 중 @Controller 어노테이션이 선언된 클래스를 빈으로 생성한다.-->
	<context:component-scan base-package="command">
		<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller" />
		<!-- <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"/> 
			<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/> -->
	</context:component-scan>

	<mvc:view-controller path="/n" view-name="n" />
	
	<mvc:view-resolvers>
		<mvc:jsp prefix="/WEB-INF/view/" />
	</mvc:view-resolvers>

</beans>